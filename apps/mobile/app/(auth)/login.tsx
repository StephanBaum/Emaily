/**
 * Login route - uses the LoginScreen component from src/screens
 *
 * This file connects the Expo Router route to the reusable
 * LoginScreen component which handles OAuth authentication
 * for Google and Microsoft accounts.
 */
import { LoginScreen } from '../../src/screens/LoginScreen';

export default LoginScreen;
